version: '3.8'
services:
  os:
    image: bwtang/xv6-env
    container_name: risc
    volumes:
      - ./:/risc
    working_dir: /risc
    command: bash
    cpus: '1'  # 限制最多使用 1 个 CPU 的算力
    restart: unless-stopped
    stdin_open: true
    tty: true
